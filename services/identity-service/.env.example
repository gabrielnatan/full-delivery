# identity-service — copie para .env e ajuste os valores
PORT=3001

DATABASE_URL=postgresql://full_delivery:full_delivery@localhost:5432/full_delivery?schema=identity

JWT_SECRET=change-me-use-openssl-rand-base64-32
